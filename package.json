{
  "name": "thaicleftlink-dashboard-checker",
  "version": "1.0.0",
  "description": "Automated health check for ThaiCleftLink Treatment Dashboard module",
  "scripts": {
    "test": "npx playwright test",
    "report": "npx tsx tests/generate-report.ts",
    "pdf": "npx tsx tests/export-pdf.ts",
    "auto-check": "npx tsx tests/run-check.ts && npx tsx tests/generate-report.ts && npx tsx tests/export-pdf.ts"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.0",
    "tsx": "^4.19.0"
  },
  "dependencies": {
    "@types/nodemailer": "^7.0.11",
    "@types/pdfkit": "^0.17.5",
    "dotenv": "^17.3.1",
    "nodemailer": "^8.0.4",
    "pdfkit": "^0.18.0"
  }
}
